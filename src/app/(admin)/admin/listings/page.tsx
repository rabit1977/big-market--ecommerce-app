import { AdminListingsHub } from '@/components/admin/admin-listings-hub';

export const metadata = {
  title: 'Manage Listings | Admin',
};

export default function AdminListingsPage() {
  return <AdminListingsHub />;
}
