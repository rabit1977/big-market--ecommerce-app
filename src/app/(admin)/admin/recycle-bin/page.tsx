import { AdminRecycleBinClient } from '@/components/admin/admin-recycle-bin-client';

export const metadata = {
  title: 'Recycle Bin | Admin',
};

export default function RecycleBinPage() {
  return <AdminRecycleBinClient />;
}
